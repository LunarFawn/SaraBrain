# v033 — The fully reproducible HamRoby loop

**Date:** 2026-05-03
**Branch:** `feature/grammar-cortex`
**Builds on:** [v028_multi_layer_cortex_architecture.md](v028_multi_layer_cortex_architecture.md),
[v031_train_l2_plan.md](v031_train_l2_plan.md)

## The commitment

HamRoby's whole architectural argument is *no opaque sources, no
vendor lock-in, no third party in the inference loop, no third party
in the training loop either*. The L1/L2/L3 stack already gives this
for L1 and L2 — UD treebanks are open, content is user-owned. The
remaining open question was the synthesizer head (HamRobySum).

**The commitment this doc makes:** HamRobySum, like everything else,
is trained from open data + the user's own substrate. **No frontier
model is required to build, retrain, or extend any HamRoby
component.** Anyone with a GPU and the open inputs below can clone
the recipe end-to-end.

This explicitly forecloses v028's Path 2 (frontier-distilled
HamRobySum) as the canonical path. Path 2 remains *available* as a
quality polish for users who want it and are willing to pay the API
cost — but the canonical, supported, advertised loop is Path 1.

## Open inputs anyone needs

- **UD treebanks** — universaldependencies.org, CC-licensed, ~MBs
- **PyTorch** — open-source
- **spaCy** + `en_core_web_sm` — open-source, small download
- **Python 3.12** — open-source
- A GPU (3070-class is enough; CPU works for inference)
- The user's own `brain.db` — their data, their facts, never shared

That's the entire dependency list. No API keys. No accounts. No
"generated by" attribution to any third party.

## The full loop

```
┌──────────────────────────────────────────────────────────────────────┐
│  STEP 1 — Train L1 (universal grammar, structural vocab)             │
│    .venv/bin/python -m sara_brain.cortex.transformer.train \\         │
│        --preset base --steps 20000                                    │
│    → grammar_base_NNNNNN.pt  (~125M params, vocab=76)                │
│    Cost: ~50 min on a 3070, $0                                       │
│    Status: ✅ shipped                                                │
└──────────────────────────────────────────────────────────────────────┘
                                   │
                                   ▼
┌──────────────────────────────────────────────────────────────────────┐
│  STEP 2 — Train L2-en (function-word adapter)                        │
│    .venv/bin/python -m sara_brain.cortex.transformer.train_l2 \\      │
│        --grammar-ckpt src/sara_brain/cortex/checkpoints/grammar_base_NNNNNN.pt
│    → l2_en_NNNNNN.pt  (134K trainable params on top of frozen L1)    │
│    Cost: ~5-6 min on a 3070, $0                                      │
│    Status: ✅ shipped (commit 52d176b, see v031)                     │
└──────────────────────────────────────────────────────────────────────┘
                                   │
                                   ▼
┌──────────────────────────────────────────────────────────────────────┐
│  STEP 3 — Generate (edges → prose) pairs from YOUR brain (labeler)   │
│    .venv/bin/python -m sara_brain.cortex.transformer.synth_data \\    │
│        --brain /path/to/your/brain.db                                 │
│        --output pairs.jsonl                                           │
│    → ~600-N pairs depending on brain size                            │
│    Cost: seconds, $0                                                  │
│    Status: ✅ shipped (synth_data.py exists, post-v032 emits         │
│              L2-grammatical prose via render_edges)                  │
└──────────────────────────────────────────────────────────────────────┘
                                   │
                                   ▼
┌──────────────────────────────────────────────────────────────────────┐
│  STEP 4 — Train HamRobySum (the synthesizer head)                    │
│    .venv/bin/python -m sara_brain.cortex.transformer.train_synth \\   │
│        --grammar-ckpt grammar_base_NNNNNN.pt \\                       │
│        --l2-ckpt l2_en_NNNNNN.pt \\                                   │
│        --pairs pairs.jsonl                                            │
│    → hamroby_sum_NNNNNN.pt  (small head on top of frozen L1+L2)      │
│    Cost: ~1-3 hours on a 3070, $0                                    │
│    Status: 🔧 NOT YET BUILT — slices below                           │
└──────────────────────────────────────────────────────────────────────┘
                                   │
                                   ▼
┌──────────────────────────────────────────────────────────────────────┐
│  STEP 5 — Use HamRoby end-to-end                                     │
│    .venv/bin/python -m sara_brain.cortex.transformer.chat \\          │
│        --brain your.db --use-hamrobysum                              │
│    No third party in the loop. Your weights, your substrate.         │
│    Status: 🔧 needs the chat-flag wiring after Step 4 lands          │
└──────────────────────────────────────────────────────────────────────┘
```

## How HamRobySum learns without a frontier teacher

The labeler IS the teacher. `synth_data.py` already emits
`(edges → prose)` pairs from any `brain.db` using the post-v032
template renderer. The student learns:

- Which edges to combine and in what order (templates render
  edge-by-edge; the student sees the whole cluster)
- Which redundant edges to drop
- Which sentence shapes are most natural for which edge types

L2-en handles function-word grammar underneath. The student inherits
`a` / `an` / `the` / agreement from L2 and only has to learn
substrate-stitching.

What the student *can't* do that a frontier-distilled student could:

- Coin novel paraphrases the templates never showed (e.g. parenthetical
  construction, rich subordination)
- Use varied register / stylized prose

For HamRoby — designed to surface substrate facts honestly, not
produce stylized prose — those losses are mostly fine.

## Step 4 sub-slices (what's missing)

This doc enumerates Step 4 as a sequence of small, individually
shippable slices. Each is its own commit + plan doc.

### 4a — `vocab_synth.py`

Extends `vocab_en` with structural delimiters used in the synth-head's
prompt format:

- `<facts>`, `<prose>`, `</prose>` — top-level frame markers
- `<subj>`, `<pred>`, `<obj>` — per-edge field markers
- `<edge_sep>` — between edges in a cluster
- `<refuted>` — flag for refuted edges
- Possibly `<topic>` for topic-hinted clusters

~10-15 new tokens. Same append-above-existing-IDs pattern as
`vocab_en.py`. New `VOCAB_SIZE_SYNTH` ≈ 190.

### 4b — Serializer in `synth_data.py`

Converts each `SynthExample` (edges + prose) into a flat token stream:

```
<facts>
  <subj> inertia in rna <pred> is <obj> tendency to maintain current state
  <subj> ribosome <pred> part_of <obj> cell
<prose>
Inertia in rna is a tendency to maintain current state. Ribosome is part of cell.
</prose>
```

Substrate labels tokenize as a sequence of word tokens (whitespace
split + lowercase). For tokens not in the vocabulary, fall back to
character bytes — keeps the model from inventing identifiers but
allows OOV substrate content to round-trip.

Output: a JSONL file where each line is the input/target token-id
sequence ready for next-token training.

### 4c — `train_synth.py`

Mirrors `train_l2.py`. Loads `(L1, L2-en)`, projects into a
synth-vocab model, freezes L1 + L2 transformer blocks, trains:

- The new delimiter token embeddings
- The LM head (weight-tied to embeddings, so trains automatically)
- Optionally the top transformer block(s) — escalate if frozen
  setting under-fits

Trains on the JSONL from 4b. Standard cross-entropy next-token loss.
Dev split: hold out 10% of clusters from each brain.

### 4d — `inference_synth.py`

Adapter analogous to `inference_l2.py`. Takes an edge cluster, formats
the `<facts>...<prose>` prefix, decodes to `</prose>`, returns the
prose between markers.

Falls back to the v032 template renderer when:
- The synth checkpoint isn't available
- The prefix exceeds the model's `max_seq`
- Decoding hits `</prose>` immediately (degenerate case)

### 4e — Chat REPL integration

`chat.py` gains a `--use-hamrobysum` flag (and a matching
`--hamrobysum-ckpt`). When set, the synthesis stage calls
`inference_synth` instead of `synthesizer.render_edges`. The template
path stays as the default fallback.

### 4f — Side-by-side eval script

A tiny script that takes a list of substrate questions, runs each
through both the template path and the HamRobySum path, prints both
outputs side by side. No accuracy autograder — the user judges
naturalness vs. grounding by eye, since "natural prose" is exactly
the kind of thing where automated metrics mislead.

## What this means for the project's pitch

After Step 4 lands, HamRoby's pitch becomes:

> A grammar transformer + per-language function-word adapter +
> substrate-aware synthesizer. ~125M params total. Runs on a 3070.
> Trained from scratch on Universal Dependencies + your own
> brain.db. Zero API. Zero accounts. Zero opaque dependencies.
> Anyone can rebuild the whole stack from this repo.

That's the architecture v028 promised. Step 4 is what closes the
loop. Path 2 (frontier distillation) becomes a footnote: an optional
quality polish for users who specifically want it and are willing to
trade reproducibility for a bit more phrasing variety.

## Order of operations

1. ✅ Save this commitment.
2. ✅ **4a** — `vocab_synth.py` (commit `67832d3`).
3. ✅ **4b** — serializer in `synth_data.py` (commit `67832d3`).
4. ✅ **4c** — `train_synth.py` (commit `0f19fd0`). Trained
   `hamroby_sum_synth_pairs_002000.pt` on /tmp/sara_demo.db
   (aptamer brain, 669 pairs).
5. ✅ **4d** — `inference_synth.py` (commit `0a341f5`).
6. ⏸️ **4e** — chat REPL integration. **Deferred** — see v0
   quality note below.
7. ⏸️ **4f** — eval script. **Deferred** for the same reason.

## v0 quality — honest status

The full Path 1 loop runs end to end. The trained checkpoint exists
and `inference_synth` produces output. But the output **isn't ready
to ship into the chat REPL** as the synthesis stage yet.

Final 2000-step training on the aptamer brain (RTX 3070, ~5.5 min):
- pre-train dev_loss=8.5963 (random embeddings for new content rows)
- step 2000: train_loss=1.83, dev_loss=4.13 (dev_ppl ≈ 62)
- Significant overfitting (train ppl ~6 vs dev ppl ~62) on the
  small ~600-pair corpus

Sample output, 8 random clusters:

| Input cluster | HamRobySum prose |
|---|---|
| `molecular snare static stems --has_formed--> no fmn state` | `Bind loop molecular snare.` |
| 11 edges about `rna stability` | `Rna stability affect and is a rna aptamer stability affect for rna stability affect for rna stability affect and is a rna stability for rna aptamer stability stability for rna stability stability stability crucial and is a rna stability stability affect to forces. ...` (repetition loop) |
| 3 edges about `5'` (constituent decomp) | `3' of 5'3' static stem.` |
| `loop around detected molecule in bound state --forms--> molecular snare` | `Molecular snare molecule snare step molecular snare.` |
| 10 `more --part_of-->` decomp edges | `Forces, is part of rna rna rna forces, is part of rna rna forces, is part of rna rna aptamer to rna ...` (repetition loop) |
| 3 edges about `rna molecules` (Newton's laws) | `Rna aptamer design law. rna. rna aptamer design introduces the study.` |

Pattern: short clean outputs are mostly fine; longer outputs spiral
into repetition. Classic small-corpus / frozen-base degeneration.

### Why this is the expected v0 baseline

- **The corpus is tiny.** ~600 (edges → prose) pairs from a single
  brain. No augmentation.
- **The base is fully frozen.** L1+L2 transformer blocks were trained
  on UD treebank conversational English; the edges→prose distribution
  is very different and the frozen blocks can't adapt.
- **Only 134K embedding params can adapt** — they have to express
  every new substrate concept's representation AND learn how those
  representations interact with the prose target. Asking a lot.
- **No stop bias / length penalty** in decoding — once the model
  starts looping it has no incentive to break out.

### What would make v1 actually shippable

Concrete experiments, in increasing cost:

- **a) Unfreeze top 1-2 transformer blocks** during synth training.
  Lets the base adapt to the synthesis task. ~30 min training, modest
  ckpt-size growth. Cheapest single fix.
- **b) Train against multiple brains** in one synth run. The current
  `--pairs` arg takes one JSONL; extending to a list would let the
  model see varied content distributions and reduce per-brain overfit.
  ~hour of code + retrain.
- **c) Augment the labeler with shuffles + sub-sampling.** Each
  cluster contributes 3-5 (edges, prose) variants instead of 1.
  Makes the 600-pair corpus look like 1800-3000 from the training
  perspective.
- **d) Add a length penalty / repetition penalty in inference**
  decoding. Cheap, helps a lot with the loops.
- **e) Mid-training: use teacher forcing on facts + free-run on prose.**
  Currently we condition on the entire facts prefix and predict the
  full prose. Could add curriculum that gradually transitions.

(a) + (d) is probably the right next slice — small code surface,
high probability of meaningful improvement.

### Decision point

Three reasonable paths from here:

1. **Iterate on Path 1 quality** before integration: do (a) + (d) +
   maybe (c). Maybe one more weekend's work.
2. **Accept v0 as a working-but-not-shippable proof of pipeline.**
   The architecture is validated end-to-end; ship what we have, mark
   v033 status as "loop closed, quality TBD", revisit.
3. **Reach for Path 2 (frontier distillation)** despite v033's
   commitment — Max plan + API spend buys richer training signal.
   Trades reproducibility purity for shipped quality.

The v033 commitment argues for option 1. Option 2 is also fine —
the *architecture* is what's load-bearing for the project's pitch;
the per-brain HamRobySum quality is a tuning concern.

## Status

- Slices 4a–4d shipped: vocab_synth, serializer, trainer, inference
  adapter all working end-to-end against /tmp/sara_demo.db.
- Slice 4e (chat REPL `--use-hamrobysum` flag) deferred until
  HamRobySum quality clears the v032 template baseline.
- Slice 4f (eval script) deferred for the same reason — would
  measure something not yet meaningful.
